/**
 * みんなの声（完了サマリーに1件だけ出す、他ユーザーからの応援メッセージ）。
 *
 * 投稿・抽選・通報の仕組みはまだ無く、いまは下の固定値だけを表示している。本実装では
 * セッションに紐付けた messageId から引く形になり、この型はそのクエリの戻り値になる。
 */
export type CommunityMessage = {
  body: string;
  author: string;
  likeCount: number;
};

/**
 * 表示を確認するための固定値。**Phase 2で実データに差し替える際に丸ごと削除すること。**
 *
 * 実装では、開き直しても同じメッセージが出るようセッションに固定する方針が確定済み。
 * 編集とサマリーを往復するたびに引き直せると、動画視聴で差し替えるリワードの意味が
 * 無くなるため（残り回数の表示は再抽選モーダルの中だけ、という方針も併せて確定）。
 *
 * これは架空の投稿で、他ユーザーの発言として表示される。実データ化・通報導線が入るまでは
 * 公開ビルドに載せないこと
 */
export const PLACEHOLDER_COMMUNITY_MESSAGE: CommunityMessage = {
  body: 'えらすぎる…わたし今日は行く気力がなくて家でアイス食べてました',
  author: '匿名さん',
  likeCount: 91,
};
